@inherits LayoutComponentBase

<MudThemeProvider Theme="_customTheme" />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudMainContent Class="pa-0" Style="height: 100vh; background-color: white;">
        @Body
    </MudMainContent>
</MudLayout>

@code {
    private MudTheme _customTheme = new MudTheme()
        {
            PaletteLight = new PaletteLight()
            {
                Primary = "#1a237e",
                Secondary = "#ff9800",
                AppbarBackground = "#ffffff",
            }
        };
}